import { createTheme, Theme } from '@material-ui/core';

const gnosisTheme = createTheme({
  palette: {
    primary: {
      main: '#002946',
    },
    secondary: {
      main: '#F5B63B',
    },
  },
});

export default gnosisTheme;
