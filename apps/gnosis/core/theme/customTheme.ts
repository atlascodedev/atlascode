import { createTheme } from '@material-ui/core';

const theme = createTheme({
  palette: {
    primary: {
      main: '#002946',
    },
    secondary: {
      main: '#FF9700',
    },
  },
});

export default theme;
